name: Release

on:
  push:
    branches:
      - main

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Update npm
        run: npm install -g npm@latest

      - name: Check Version
        id: check
        run: |
          PACKAGE=$(node -p "require('./package.json').name")
          LOCAL=$(node -p "require('./package.json').version")
          PUBLISHED=$(npm view "$PACKAGE" version 2>/dev/null || echo "0.0.0")

          echo "package=$PACKAGE"
          echo "local=$LOCAL"
          echo "published=$PUBLISHED"

          if [ "$(npx semver "$LOCAL" -r ">$PUBLISHED")" ]; then
            echo "should_publish=true" >> "$GITHUB_OUTPUT"
          else
            echo "should_publish=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Install Dependencies
        run: npm ci

      - name: Build
        run: npm run build
      
      - name: TypeCheck
        run: npm run typecheck

      - name: Test
        run: npm test

      # `Check Version` decides; without this gate every push to main tries to
      # publish, and fails whenever the version was not bumped.
      - name: Publish
        if: steps.check.outputs.should_publish == 'true'
        run: npm publish --provenance